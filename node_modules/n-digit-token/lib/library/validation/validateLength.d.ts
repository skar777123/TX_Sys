/**
 * Validates input length.
 * Throws an error if length is not a positive integer.
 * @param {number} length
 * @throws {error} if not called with a positive integer
 */
export declare const validateLength: (length: number) => void;
